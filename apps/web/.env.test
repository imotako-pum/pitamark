VITE_API_URL=http://localhost:8787
# Phase 7: Turnstile dev test key (always passes). Required for E2E so the
# upload form gets a token without contacting Cloudflare.
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA
